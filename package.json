{
  "name": "typescript-zen",
  "version": "1.0.0",
  "description": "Simple typescript app with dynamic css change in html",
  "scripts": {
    "compile": "tsc",
    "watch": "tsc --watch",
    "run": "browser-sync --start --server --watch"
  },
  "author": "Jakub Paszkiewicz",
  "dependencies": {
    "browser-sync": "^2.27.7",
    "typescript": "^4.5.4"
  }
}
